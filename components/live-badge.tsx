import { cn } from "@/lib/utils";


interface LiveBadgeProps {
    className?: string;
};

export const LiveBadge = ({
    className,
}: LiveBadgeProps) => {
    return (
        <div>

        </div>
    )
}